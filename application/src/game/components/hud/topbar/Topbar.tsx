import { Navigation } from './Navigation';
import { ResourceDisplay } from './ResourceDisplay';

export const Topbar = () => {
    return (
        <>
            <Navigation />
            <ResourceDisplay />
        </>
    );
};
